apps:
  # - script: ./dist/schedules/worker.js
  #   name: 'stream-worker'
  #   watch: false
  #   exec_mode: fork
  #   env:
  #     NODE_ENV: production
  #   env_production:
  #     NODE_ENV: production

  - script: ./dist/schedules/streamKline.js
    name: 'stream-kline'
    watch: false
    exec_mode: fork
    env:
      NODE_ENV: production
    env_production:
      NODE_ENV: production

  - script: ./dist/schedules/streamTicker.js
    name: 'stream-ticker'
    watch: false
    exec_mode: fork
    env:
      NODE_ENV: production
    env_production:
      NODE_ENV: production
